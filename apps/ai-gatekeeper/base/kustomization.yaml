apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai-gatekeeper

resources:
  - namespace.yaml
  - config
  - postgres
  - migrator
  - redis
  - proxy
  - web
  - ingress
  - network-policies
