apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai-gatekeeper-production

resources:
  - ../../base
  - proxy-pdb.yaml
  - web-pdb.yaml

patchesStrategicMerge:
  - ingress-tls-patch.yaml

replicas:
  - name: web
    count: 2
  - name: proxy
    count: 2
